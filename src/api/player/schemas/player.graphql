# import * from "./../../person/schemas/person.graphql"
# import * from "./../../player-position/schemas/player-position.graphql"

type Player {
  id: Int!
  foot: String!
  active: Boolean!
  person: Person!
  playerPositions: [PlayerPosition!]!
}

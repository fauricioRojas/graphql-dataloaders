# import * from "./../../person/schemas/person.graphql"
# import * from "./../../position/schemas/position.graphql"

type Player {
  id: Int!
  foot: String!
  active: Boolean!
  person: Person!
  positions: [Position!]!
}

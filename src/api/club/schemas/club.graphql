# import * from "./../../country/schemas/country.graphql"
# import * from "./../../club-player/schemas/club-player.graphql"

type Club {
  id: Int!
  country: Country!
  name: String!
  clubPlayers: [ClubPlayer]!
}